# install
make sure to install <code>echo "# site-luca" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/lucamoraru/site-luca.git
git push -u origin main</code>

<input type="file"> choose a file
